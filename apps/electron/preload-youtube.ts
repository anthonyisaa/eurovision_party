// Preload script for the YouTube BrowserWindow.
//
// Agent E will use this to scrape the YouTube player's currentTime each tick
// and post it to the overlay window (which then writes it to Supabase). For
// now this is a stub so the file path is reserved.

export {};
