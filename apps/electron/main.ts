// Eurojury electron stub.
//
// Agent E replaces this with the real implementation: a fullscreen YouTube
// BrowserWindow plus a transparent always-on-top overlay window pointed at
// /tv?electron=1. For now we just log so `pnpm dev:electron` proves the
// workspace plumbing works.

console.log('electron stub ready');
process.exit(0);
